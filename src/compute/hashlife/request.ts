import { Cell } from './cellType'
import { Pair } from '../../util/RectType'
import { Policy } from './policyType'

export let request = (
   cell: Cell,
   policy: Policy,
   posYX: Pair,
   buffer,
   bufferSizeYX,
) => {
   if (cell.level) {
   }
}
