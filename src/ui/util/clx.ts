/**
 * clx
 * A short helper function to join texts on space. It can be used to join
 * class names
 *
 * @param text the text fragments to join with a space
 */
export let clx = (...text: string[]) => text.join(' ')
